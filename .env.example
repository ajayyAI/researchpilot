# ── Required ──────────────────────────────────────────

FIRECRAWL_API_KEY=           # https://firecrawl.dev

# Pick ONE provider and set its key:
AI_PROVIDER=openrouter       # openai | openrouter | groq
OPENROUTER_API_KEY=          # https://openrouter.ai/keys
# OPENAI_API_KEY=            # https://platform.openai.com/api-keys
# GROQ_API_KEY=              # https://console.groq.com

# ── Model Tiers (optional) ───────────────────────────
# All tiers default to the provider's best model.
# Override per tier to optimize cost vs quality.
#
# AI_MODEL=gpt-4.1                  # default for all tiers
# AI_MODEL_FAST=gpt-4.1-mini        # extraction, source scoring
# AI_MODEL_STRATEGIC=o4-mini        # planning, reasoning

# ── Search (optional) ────────────────────────────────
# TAVILY_API_KEY=                    # https://tavily.com — adds a second search source
# FIRECRAWL_CONCURRENCY=2           # increase with paid Firecrawl plan

# ── Site ──────────────────────────────────────────────
# NEXT_PUBLIC_SITE_URL=https://researchpilot.dev
